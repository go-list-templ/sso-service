apiVersion: v1
kind: Service
metadata:
  name: sso-service
spec:
  selector:
    app: sso-service
  ports:
    - name: grpc
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081