apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sso-service

resources:
  - job/wait-depends.yaml
  - job/migrate.yaml
  - configmap.yaml
  - deployment.yaml
  - sa.yaml
  - service.yaml
